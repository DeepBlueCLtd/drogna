# Node
node_modules/
.pnpm-store/
app/dist/
.vite/

# Playwright / capture artefacts that are not durable (SRD-v2 FR-19).
# A glance lives for the session; a candidate becomes durable by being moved into the
# site by hand, which is the review gate, and never by being written there.
/.capture/
app/e2e/.artifacts/
test-results/
playwright-report/

# Local environment
*.env
.env

# Editors and OS
.DS_Store
.idea/
