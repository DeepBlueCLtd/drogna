{
  "component": {
    "id": "common",
    "description": "The destination's root seed, which deploy/lib/seed_record.py reads and nothing else does. The clock and logging blocks below are required by the schema and are read by no component: there is no merge step, and every component's own file carries its complete values. See ADR-0018."
  },
  "clock": {
    "endpoint": "http://clock:8090",
    "routes": {
      "snapshot": "/clock/snapshot",
      "control": "/clock/control"
    },
    "mode": "accelerated",
    "stale_after_gap": 4,
    "timeout_seconds": 5.0
  },
  "seed": {
    "root": 20260826,
    "stream": "common"
  },
  "logging": {
    "level": "INFO"
  }
}
