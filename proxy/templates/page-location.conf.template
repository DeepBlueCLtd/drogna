    location @{modifier} @{location_path} {
        set $harness_rule "@{rule}";
        proxy_pass @{upstream};
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
