// Perturbation drawn from the platform's entropy rather than from a named stream.
// Planted for scripts/tests/test_gates_fail.py; the `.fixture` suffix keeps it out of
// the repository-wide walk.
export function perturb(value: number): number {
  return value + Math.random();
}
