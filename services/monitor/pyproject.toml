[project]
name = "harness-monitor"
version = "0.1.0"
description = "C-11: the divergence monitor, which owns the failure mode of over-sensitivity"
requires-python = ">=3.11"
dependencies = [
    "harness-core",
    "harness-types",
]

[tool.uv.sources]
harness-core = { workspace = true }
harness-types = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/harness_monitor"]
