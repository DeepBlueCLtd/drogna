[project]
name = "harness-scheduler"
version = "0.1.0"
description = "C-12: the run scheduler, which owns the failure mode of thrashing"
requires-python = ">=3.11"
dependencies = [
    "harness-core",
    "harness-types",
]

[tool.uv.sources]
harness-core = { workspace = true }
harness-types = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/harness_scheduler"]
