[project]
name = "harness-sensors"
version = "0.1.0"
description = "C-04: simulated sensors publishing observations in SensorThings vocabulary on the observation branch"
requires-python = ">=3.11"
dependencies = [
    "harness-core",
    # The generated payload types. Constitution III: the observation message is defined
    # once, in JSON Schema, and both ends of the path use what is generated from it.
    "harness-types",
    # C-02's package, for the evaluator that says what the environment is at a point. A
    # sensor measures the generated field; a second evaluation here would be a second
    # environment, and the ground truth AT-03 scores against would no longer be one thing.
    # harness:allow-service-dependency a sensor samples the field C-02 authored, not a second one
    "harness-env-generator",
]

[tool.uv.sources]
harness-core = { workspace = true }
harness-types = { workspace = true }
harness-env-generator = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/harness_sensors"]
