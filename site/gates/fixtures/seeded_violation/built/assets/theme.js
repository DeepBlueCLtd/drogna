// Deliberate control. Emitted-script zone: a data model leaking into published
// JavaScript, which is a different claim from prose about the prohibition.
const seededPanel = { detection_id: "seeded-control", tracklets: 4 };
export default seededPanel;
