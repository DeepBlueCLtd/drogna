# Fully pinned site build environment, including transitive dependencies, so the
# local build and the publishing workflow produce the same output. Regenerate
# with: pip install mkdocs-material==<version> && pip freeze
#
# Direct dependencies:
#   mkdocs, mkdocs-material  — the site generator and theme
#   pymdown-extensions       — markdown extensions the theme relies on
#   ghp-import               — used by `mkdocs gh-deploy` to write gh-pages
#
# Everything else below is transitive and pinned for reproducibility.
babel==2.18.0
backrefs==8.0
certifi==2026.7.22
charset-normalizer==3.5.1
click==8.5.0
colorama==0.4.6
ghp-import==2.1.0
idna==3.19
Jinja2==3.1.6
Markdown==3.10.3
MarkupSafe==3.0.3
mergedeep==1.3.4
mkdocs==1.6.1
mkdocs-get-deps==0.2.2
mkdocs-material==9.7.7
mkdocs-material-extensions==1.3.1
packaging==26.3
paginate==0.5.7
pathspec==1.1.1
platformdirs==4.11.4
Pygments==2.21.0
pymdown-extensions==11.0.2
python-dateutil==2.9.0.post0
PyYAML==6.0.3
pyyaml_env_tag==1.1
requests==2.34.2
six==1.17.0
urllib3==2.7.0
watchdog==6.0.0
